%h1 Listing trends

%table
  %thead
    %tr
      %th Trend
      %th Currency Pair
      %th Fundamental
      %th Profit
      %th

  %tbody
    - @trends.each do |trend|
      %tr
        %td= trend.kind
        %td= trend.rate.currency_pair.display_name
        %td
          = trend.deal.try(:fundamental) || '-'
        %td
          = trend.deal.try(:profit) || '-'
        %td= link_to 'Edit', edit_trend_path(trend)

= link_to 'logout', destroy_user_session_path, method: :delete
